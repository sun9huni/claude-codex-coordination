# Identical to good_cluster_then_grid.yaml except fragmap_npz points at a
# nonexistent path (the validator must flag the missing NPZ as an error).
fragmap_conditioning:
  enabled: true
  mode: cluster_then_grid
  fragmap_npz: /mnt/data/does/not/exist_maps.npz
  reference_pdb: /mnt/data/users/kim/code/FKSFold-Boltz_Advancement/data/silcs_oracle_real/ternary_r2/ternary_r2_equil.pdb
  channels:
    - acceptor_ether
    - amide_donor
    - amide_acceptor
